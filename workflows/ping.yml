name: Keep Render Alive

on:
  schedule:
    - cron: "*/5 * * * *"   # 5 dakikada bir
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest

    steps:
      - name: Ping server
        run: |
          curl -fsS https://larp-assistance.onrender.com/ > /dev/null || exit 1
